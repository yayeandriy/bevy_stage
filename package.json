{
  "name": "spector_id",
  "version": "1.0.0",
  "description": "Template for a Game using the awesome [Bevy engine][bevy] featuring out of the box builds for Windows, Linux, macOS, Web (Wasm), Android, and iOS.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "cp -r .vercel dist && cp vercel.json dist && cd dist && vercel --prod",
    "build": "trunk build --release",
    "postinstall": "cargo install trunk"
  }
  ,"keywords": [],
  "author": "",
  "license": "ISC"
}
